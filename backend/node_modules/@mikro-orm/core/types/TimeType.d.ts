import { Type } from './Type.js';
import type { Platform } from '../platforms/Platform.js';
import type { EntityProperty } from '../typings.js';
/** Maps a database TIME column to a JS `string` in HH:MM:SS format. */
export declare class TimeType extends Type<string | null | undefined, string | null | undefined> {
    convertToDatabaseValue(value: string | null | undefined, platform: Platform): string | null | undefined;
    compareAsType(): string;
    ensureComparable(): boolean;
    getColumnType(prop: EntityProperty, platform: Platform): string;
    getDefaultLength(platform: Platform): number;
}
